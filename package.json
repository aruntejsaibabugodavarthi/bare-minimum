{
  "name": "bare-minimum-monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev": "npm run dev -w apps/api & npm run dev -w apps/web & npm run dev -w apps/old-web & npm run dev -w apps/admin & wait",
    "build": "npm run build --workspaces --if-present",
    "install:all": "npm install"
  }
}
